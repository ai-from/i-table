export type PictureType = {
  large: string
  medium: string
  thumbnail: string
}

export type NameType = {
  title: string
  first: string
  last: string
}

export type LocationType = {
  country: string
}

export type DOBType = {
  date: string
}

export type filterType = {
  from: number
  count: number
}
